<% if (site.posts.length){ %>
  <div class="widget-wrap">
    <!-- <h3 class="widget-title"><%= __('month_archive') %></h3> -->
    <div class="widget">
      <%- list_archives({show_count: config.show_count || theme.show_count, type: config.archive_type || theme.archive_type}) %>
    </div>
  </div>
<% } %>